package com.example.labatri.repository;

import com.example.labatri.model.SLA;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SLARepository extends JpaRepository<SLA, Long> {
}
