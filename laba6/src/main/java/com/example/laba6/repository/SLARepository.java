package com.example.laba6.repository;

import com.example.laba6.model.SLA;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SLARepository extends JpaRepository<SLA, Long> {
}
